{
  "name": "platformer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "2D platformer (vanilla JS + Canvas). No build step for the game itself; this package.json exists to host the dark factory tooling (jest, optional playwright).",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "serve": "node -e \"import('http').then(({createServer}) => import('fs').then(({readFileSync,statSync,existsSync}) => import('path').then(({join,extname}) => { const mime={'.html':'text/html','.js':'application/javascript','.css':'text/css','.png':'image/png','.json':'application/json'}; createServer((req,res)=>{let p=join(process.cwd(),decodeURIComponent(req.url.split('?')[0])); if(existsSync(p)&&statSync(p).isDirectory()) p=join(p,'platformer.html'); if(!existsSync(p)){res.writeHead(404);return res.end('not found');} res.writeHead(200,{'Content-Type':mime[extname(p)]||'application/octet-stream'}); res.end(readFileSync(p));}).listen(8123,()=>console.log('http://localhost:8123/platformer.html'));})))\""
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "<rootDir>/tests/unit/**/*.test.js"
    ],
    "transform": {},
    "moduleFileExtensions": ["js", "mjs"],
    "setupFiles": ["<rootDir>/tests/jest.setup.js"]
  }
}
